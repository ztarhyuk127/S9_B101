using Photon.Pun;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerSpawner : MonoBehaviour
{
    // Start is called before the first frame update
    //void Start()
    //{
    //    Transform[] points = GameObject.Find("SpawnGroup").GetComponentsInChildren<Transform>();

    //    GameObject[] player = GameObject.FindGameObjectsWithTag("Player");
    //    if (player.Length != 0)
    //    {
    //        for (int i = 0; i < player.Length; i++)
    //        {
    //            int idx = Random.Range(1, points.Length);
    //            Debug.Log("º¸³Â½À´Ï´Ù");
    //            CharacterController characterController = player[i].GetComponent<CharacterController>();
    //            characterController.enabled = false;
    //            player[i].transform.position = points[idx].position;
    //            characterController.enabled = true;
    //        }
    //    }
    //}
}
